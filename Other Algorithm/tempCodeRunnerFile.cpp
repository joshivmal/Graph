    // dfs(0, -1, TOI, MTOI, vis, adj, bridges) ;

    // cout<<"BRIDGES"<<endl ;
    // for( int i=0 ; i < bridges.size() ; i++){
    //     cout<<bridges[i].first<<" "<<bridges[i].second<<endl ;
    // }